political_decisions = {

	FLD_accept_almalayikan = {
		picture = embrace_minority
		potential = { 
			tag = FLD
			NOT = { has_country_flag = accept_almalayikan }
		}

		allow = {
			war = no
		}

		effect = { 
			prestige = 5
			set_country_flag = accept_almalayikan	
			add_accepted_culture = noachis_almalayikan
		}
	}

	FLD_inherit_BOS = {
		picture = double_down_in_the_balkans
		potential = { 
			tag = FLD
			NOT = { has_country_flag = annex_bosphorus }
		}

		allow = {
			war = no
			state_n_government = 1
			BOS = { vassal_of = THIS }
		}

		effect = { 
			prestige = 5
			set_country_flag = annex_bosphorus	
			add_accepted_culture = noachis_argyrian
			BOS = {
				country_event = 50013
				all_core = {
                    		add_core = FLD
                    		remove_core = BOS
				    }
		        	}
			}
		}

	FLD_inherit_NRP = {
		picture = begin_cultuurstelsel
		potential = { 
			tag = FLD
			NOT = { has_country_flag = annex_nereidum }
		}

		allow = {
			war = no
			nationalism_n_imperialism = 1
			NRP = { vassal_of = THIS }
		}

		effect = { 
			prestige = 5
			set_country_flag = annex_nereidum
			add_accepted_culture = noachis_argyrian
			NRP = {
				country_event = 50013
				all_core = {
                    		add_core = FLD
                    		remove_core = NRP
				    }
		        	}
			}
		}
}